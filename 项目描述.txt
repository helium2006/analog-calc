这是一个辅助计算fet不同状态的相关参数的界面程序，开始的fet类型选择，参数填写，会存入一个叫params的字典，这个字典包含了
这次运算的全部信息，包括与fet管对应的特定参数，计算特定工作模式（amplifier use）时的必须参数，全部的操作均基于刚开始
定义好的params，从params中读取需要的参数，没有读取得到返回提示框，提示用户当前缺少的参数


全部的信息获取和重要的计算中间量，均会保存进入params。